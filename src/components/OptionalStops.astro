---
interface Props {
  rideDay: string;
}

const { rideDay } = Astro.props;

// Empty component - Optional Stops will be implemented separately
// without modifying the build pipeline
---

<!-- Placeholder for future Optional Stops implementation -->
